<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration>

<configuration scan="true">
    <conversionRule conversionWord="crlf" converterClass="com.tulip.host.config.CRLFLogConverter"/>

    <!--
        Human-readable console pattern for dev.
        Uses standard Spring Boot level coloring (%clr(%5p) colours per level).
        %crlf sanitises log-injection characters without forcing an arbitrary colour.
    -->
    <property name="CONSOLE_LOG_PATTERN"
              value="${CONSOLE_LOG_PATTERN:-%clr(%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX}){faint} %clr(%5p) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} : %crlf(%m) %n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}}"/>

    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <!-- ═══════════════════════════════════════════════════════
         Logger level configuration (all profiles)
         ═══════════════════════════════════════════════════════ -->
    <logger name="com.tulip.host" level="INFO"/>

    <logger name="angus.activation"                            level="WARN"/>
    <logger name="jakarta.activation"                          level="WARN"/>
    <logger name="jakarta.mail"                                level="WARN"/>
    <logger name="jakarta.management.remote"                   level="WARN"/>
    <logger name="jakarta.xml.bind"                            level="WARN"/>
    <logger name="jdk.event.security"                          level="INFO"/>
    <logger name="io.github.resilience4j"                      level="INFO"/>
    <logger name="com.netflix"                                 level="WARN"/>
    <logger name="com.netflix.config.sources.URLConfigurationSource" level="ERROR"/>
    <logger name="com.netflix.discovery"                       level="INFO"/>
    <logger name="com.ryantenney"                              level="WARN"/>
    <logger name="com.sun"                                     level="WARN"/>
    <logger name="com.zaxxer"                                  level="WARN"/>
    <logger name="io.undertow"                                 level="WARN"/>
    <logger name="io.undertow.websockets.jsr"                  level="ERROR"/>
    <logger name="org.apache"                                  level="WARN"/>
    <logger name="org.apache.catalina.startup.DigesterFactory" level="OFF"/>
    <logger name="org.bson"                                    level="WARN"/>
    <logger name="org.hibernate.validator"                     level="WARN"/>
    <logger name="org.hibernate"                               level="WARN"/>
    <logger name="org.hibernate.ejb.HibernatePersistence"      level="OFF"/>
    <logger name="org.springframework"                         level="WARN"/>
    <logger name="org.springframework.web"                     level="WARN"/>
    <logger name="org.springframework.security"                level="WARN"/>
    <logger name="org.springframework.boot.autoconfigure.logging" level="INFO"/>
    <logger name="org.springframework.cache"                   level="WARN"/>
    <logger name="org.thymeleaf"                               level="WARN"/>
    <logger name="org.xnio"                                    level="WARN"/>
    <logger name="io.swagger.v3"                               level="INFO"/>
    <logger name="sun.rmi"                                     level="WARN"/>
    <logger name="sun.net.www"                                 level="INFO"/>
    <logger name="sun.rmi.transport"                           level="WARN"/>
    <logger name="Validator"                                   level="INFO"/>
    <logger name="_org.springframework.web.servlet.HandlerMapping.Mappings" level="INFO"/>
    <logger name="org.springframework.boot.docker"             level="WARN"/>
    <logger name="liquibase"                                   level="WARN"/>
    <logger name="LiquibaseSchemaResolver"                     level="INFO"/>
    <!-- jhipster-needle-logback-add-log -->

    <!-- ═══════════════════════════════════════════════════════
         DEV — human-readable coloured console
         ═══════════════════════════════════════════════════════ -->
    <springProfile name="dev">
        <springProperty name="log.level" source="logging.level.root" defaultValue="INFO"/>
        <root level="${log.level}">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!-- ═══════════════════════════════════════════════════════
         PROD — JSON console (picked up by Promtail → Loki)
                + async direct-push to Loki via loki4j
         ═══════════════════════════════════════════════════════
         Environment variables expected at runtime:
           LOKI_URL   - Loki push endpoint (default http://loki:3100)
           HOSTNAME   - pod/host name injected by container runtime
         ═══════════════════════════════════════════════════════ -->
    <springProfile name="prod">
        <!--
            Loki4j appender — pushes log lines directly to Loki over HTTP.
            Labels become Loki stream selectors queryable in Grafana:
              {app="tuliphost", env="prod", level="ERROR"}
            Message is logfmt so LogQL `| logfmt` parses all key=value fields.
        -->
        <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
            <http>
                <url>${LOKI_URL:-http://loki:3100}/loki/api/v1/push</url>
                <connectionTimeoutMs>5000</connectionTimeoutMs>
                <requestTimeoutMs>5000</requestTimeoutMs>
            </http>
            <format>
                <label>
                    <pattern>app=tuliphost,env=prod,level=%level,host=${HOSTNAME:-localhost}</pattern>
                    <readMarkers>true</readMarkers>
                </label>
                <!--
                    logfmt message — queryable in Grafana with:
                      {app="tuliphost"} | logfmt | level="ERROR"
                      {app="tuliphost"} | logfmt | logger=~"c.t.h.service.*"
                -->
                <message>
                    <pattern>level=%level ts=%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} app=tuliphost host=${HOSTNAME:-localhost} logger=%logger{36} thread=%thread traceId=%X{traceId:-} spanId=%X{spanId:-} msg=%crlf(%msg) %n%ex{full}</pattern>
                </message>
                <sortByTime>true</sortByTime>
            </format>
            <!-- batch up to 1 000 lines or 5 s, whichever comes first -->
            <batchMaxItems>1000</batchMaxItems>
            <batchTimeoutMs>5000</batchTimeoutMs>
            <verbose>false</verbose>
        </appender>

        <!--
            Wrap in AsyncAppender so Loki HTTP calls never block the request thread.
            neverBlock=true drops log records rather than stalling the app under backpressure.
        -->
        <appender name="ASYNC_LOKI" class="ch.qos.logback.classic.AsyncAppender">
            <queueSize>8192</queueSize>
            <neverBlock>true</neverBlock>
            <appender-ref ref="LOKI"/>
        </appender>

        <springProperty name="log.level" source="logging.level.root" defaultValue="INFO"/>
        <!--
            CONSOLE remains active: LoggingConfiguration replaces it with a JSON
            console appender when jhipster.logging.use-json-format=true, which
            Promtail then scrapes from stdout as a second path into Loki.
        -->
        <root level="${log.level}">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_LOKI"/>
        </root>
    </springProfile>

    <!-- Prevent logback from outputting its own status messages -->
    <statusListener class="ch.qos.logback.core.status.NopStatusListener"/>
    <!-- Propagate logback level changes to java.util.logging -->
    <contextListener class="ch.qos.logback.classic.jul.LevelChangePropagator">
        <resetJUL>true</resetJUL>
    </contextListener>

</configuration>
