package com.tulip.host.enums;

/**
 * Defines the maximum external marks for a TERM exam subject.
 * FULL  → External out of 80 (+ Internal out of 10)
 * HALF  → External out of 50 only — no Internal component
 */
public enum TermMarkType {
    FULL,
    HALF,
}
