package com.tulip.host.enums;

public enum ScoreType {
    TOTAL, // CT exam — single score per subject (out of 10)
    INTERNAL, // TERM exam — internal component (out of 10)
    EXTERNAL, // TERM exam — external component (out of 80)
}
